package com.company;

public class ScannerHandler { //a class that contains all the scanner data e.g. name path log path alt log path connection state (called when a new scanner gets connected)
    //if the scanner is chosen from the scanner list but not connected set connection state false and variables null
}
